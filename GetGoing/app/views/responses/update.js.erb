$("#responses").html("<%= escape_javascript(render(partial: 'responses/index', locals: { post: @post })).html_safe %>");
$("#modal-component").modal("hide");
$("#booking_links").html("<%= escape_javascript(render(partial: 'booking_links/booking_links', locals: { booking_links: @post.booking_links })).html_safe %>");
