$("#modal-step-content").hide().html("<%= escape_javascript(render( partial: 'devise/registrations/new', locals: { })).html_safe %>").fadeIn("slow");;

bar.animate(1.0);  // Number from 0.0 to 1.0

$('.modal-header h3').hide().text('Create an account').fadeIn("slow");
$('.modal-header p').hide();
