$("#modal-step-content").hide().html("<%= escape_javascript(render( partial: 'posts/post_steps/recommendations', locals: { })).html_safe %>").fadeIn("slow");
$("#modal-component").modal("show");

$('#recommendations-select').imagepicker(
  {
    show_label: true
  }
);

if ($('.modal-content').hasClass('recommendations-modal')) {
  $(".modal-content").removeClass('recommendations-modal');
}

bar.animate(0.5);  // Number from 0.0 to 1.0
$('.modal-header h3').hide().text('Recommendations').fadeIn("slow");
